package Aplicaciones;
import Clases.Recursiva;

public class MainExponencial {

    public static void main(String[] args) {
        Recursiva calculo = new Recursiva(4.0, 2);
        System.out.println(calculo.toString());
    }
}